# Release Rubric

Release only if:

- package validates,
- install path is tested,
- plugin namespace works,
- examples contain no secrets/private data,
- changelog version matches manifest,
- security defaults are unchanged or documented.
