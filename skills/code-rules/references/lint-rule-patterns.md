# Lint Rule Patterns

Use deterministic gates for:

- filenames,
- route names,
- import boundaries,
- forbidden suffixes,
- public exports,
- dependency direction,
- test file presence,
- no direct external calls outside gateway modules.

Prefer existing toolchain first: ESLint, Biome, Ruff, mypy, pytest, vitest, Playwright.
