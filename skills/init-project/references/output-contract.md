# Init Project Output Contract

A valid scaffold has:

- `harness.yaml` at project root.
- `CLAUDE.md` at project root.
- 13 docs under `docs/`.
- 6 rule files under `rules/`.
- 3 eval files under `evals/`.

All placeholders should be explicit `TODO`, not hidden omissions.
