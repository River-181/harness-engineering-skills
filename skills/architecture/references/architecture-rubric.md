# Architecture Rubric

Pass if:

- components have clear responsibilities,
- dataflow crosses labeled trust boundaries,
- external services declare data sent and fallback,
- audit/logging exists for high-risk actions,
- demo-mode shortcuts are marked.
